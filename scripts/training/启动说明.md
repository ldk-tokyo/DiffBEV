# 训练脚本启动说明

## 📍 脚本位置

所有训练脚本位于 `scripts/training/` 目录。

## 🚀 启动方式

### 1. 快速训练（推荐）

最简单快捷的启动方式，直接使用环境中的Python：

```bash
cd /media/ldk950413/data0/DiffBEV
bash scripts/training/快速训练.sh
```

**特点**:
- 自动检测环境
- 自动设置CUDA和PyTorch环境变量
- 使用配置文件 `configs/baseline/lss_swin_nuscenes.py`
- 输出目录：`runs/baseline`
- 使用GPU 0

### 2. 完整训练命令

包含环境激活和完整配置的训练启动：

```bash
cd /media/ldk950413/data0/DiffBEV
bash scripts/training/完整训练命令.sh
```

**特点**:
- 自动激活 `diffbev_py310` 环境
- 完整的CUDA和PyTorch环境变量设置
- 支持自定义配置文件和工作目录

### 3. GPU训练启动

GPU训练专用启动脚本：

```bash
cd /media/ldk950413/data0/DiffBEV
bash scripts/training/GPU训练启动脚本.sh
```

### 4. 基线训练

#### CPU基线
```bash
cd /media/ldk950413/data0/DiffBEV
bash scripts/training/run_baseline_cpu.sh
```

#### nuScenes基线
```bash
cd /media/ldk950413/data0/DiffBEV
bash scripts/training/run_baseline_nuscenes.sh
```

## 📋 训练参数

默认训练参数：
- **配置文件**: `configs/baseline/lss_swin_nuscenes.py`
- **工作目录**: `runs/baseline`
- **GPU ID**: `0`
- **评估间隔**: 每20000次迭代
- **最大迭代**: 200000次

## 💡 使用建议

1. **首次使用**: 推荐使用 `快速训练.sh`
2. **自定义配置**: 可以直接修改脚本中的参数，或传入环境变量
3. **多GPU训练**: 需要修改脚本使用 `tools/dist_train.sh`

## 🔍 查看训练状态

训练启动后，可以使用监控脚本查看训练状态：

```bash
# 实时监控
bash scripts/monitoring/实时监控训练.sh

# 或使用简洁监控
watch -n 2 -c bash scripts/monitoring/简洁监控训练.sh
```

## 📝 日志位置

训练日志保存在：
- `runs/baseline/{timestamp}/` - 训练输出目录
- `runs/baseline/*.log` - 日志文件
- `runs/baseline/best_mIoU.pth` - 最佳模型checkpoint
